# QRLockApp — Environment Variables
# Copy this file to .env and update values before running Docker

# Database
DB_HOST=db
DB_USER=qrlockuser
DB_PASS=qrlockpass
DB_NAME=qrlockapp

# MySQL Root (for Docker only)
MYSQL_ROOT_PASSWORD=rootpassword
